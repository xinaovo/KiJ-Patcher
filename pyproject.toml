[project]
name = "kijpatcher"
version = "0.9.9"
description = "A CLI tool to Patch KiCad-generated gerber file to complies with JLC rules."
authors = [
    { name="Xina", email="realxinaovo@gmail.com" }
]
readme = "README.md"
license = "GPL-2.0-only"
requires-python = ">=3.7"
dependencies = []

[project.scripts]
tooly = "kijpatcher.kijpatcher:main"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
